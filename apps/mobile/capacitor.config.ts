import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jorge.stuffingcalc',
  appName: 'Stuffing Calculator',
  webDir: 'dist'
};

export default config;
